package com.overklassniy.stankinschedule.schedule.editor.ui.components

/**
 * Режим работы редактора пары.
 *
 * Определяет, создаём новую пару или редактируем существующую.
 */
enum class EditorMode {
    /** Режим создания новой пары. */
    Create,

    /** Режим редактирования существующей пары. */
    Edit
}